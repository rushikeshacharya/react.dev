// import {createStore} from "redux";
// import counterReducer from "./reducer";

/*
  createStore is the classic API. In newer projects you might use configureStore (RTK).
  This store's state shape is the reducer's state object: { count: number }
*/

// const store = createStore(counterReducer);

// export default store;
