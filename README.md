# React ToDo

1. React Compiler
2. React new features
   1. use hook

- handleClick.bind(null, 'age') : Prepares a function that calls handleClick('age') on click
- () => handleClick('age') : Inline arrow function that calls handleClick('age') on click
- handleClick('age') directly : Calls immediately on render (❌ not what you wa
